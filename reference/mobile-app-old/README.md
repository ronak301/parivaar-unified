# Reference only — not built

This is a verbatim copy of the old `parivaar-app` React Native (Expo) source,
kept here for reference while porting logic into the unified Next.js app.

- Not part of the Next.js build — excluded from `tsconfig.json` and ESLint.
- `node_modules`, `.git`, `.netlify`, `.expo`, and native build folders were
  stripped when copying.
- Do not run, build, or deploy from this folder.
- Original README preserved as `README.original.md`.
